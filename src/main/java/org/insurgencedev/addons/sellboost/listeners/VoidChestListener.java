package org.insurgencedev.addons.sellboost.listeners;

import org.bukkit.event.Listener;

public final class VoidChestListener implements Listener {

//    @EventHandler
//    private void onSell(ArmorEquipEvent event) {
//        Player player = Remain.getPlayerByUUID(Objects.requireNonNull(event.getVoidStorage()).ownerUUID());
//        AddonUtil.getMulti(player, (found, multi) -> {
//            if (found) {
//                event.setPrice(BigDecimal.valueOf(AddonUtil.calculateAmount(event.getPrice().doubleValue(), multi)));
//            }
//        });
//    }
//
//    @EventHandler
//    private void onSell(ArmorUnequipEvent event) {
//        Player player = Remain.getPlayerByUUID(Objects.requireNonNull(event.getVoidStorage()).ownerUUID());
//        AddonUtil.getMulti(player, (found, multi) -> {
//            if (found) {
//                event.setPrice(BigDecimal.valueOf(AddonUtil.calculateAmount(event.getPrice().doubleValue(), multi)));
//            }
//        });
//    }
}
